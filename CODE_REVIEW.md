# 시스템 리소스 모니터링 시스템 - 코드 리뷰 보고서

**리뷰 날짜**: 2025-11-06
**리뷰 대상**: monitor.py (589줄)
**프로젝트**: 시스템 리소스 모니터링 시스템

---

## 📋 목차
1. [실행 결과 요약](#실행-결과-요약)
2. [코드 구조 분석](#코드-구조-분석)
3. [보안 문제점](#보안-문제점)
4. [코드 품질 문제점](#코드-품질-문제점)
5. [종합 평가 및 권장사항](#종합-평가-및-권장사항)

---

## 실행 결과 요약

### ✅ 실행 성공
- **총 실행 시간**: 120초 (2분)
- **수집 데이터**: 109개 포인트
- **PDF 생성**: 성공 (176KB)
- **경고**: matplotlib tight_layout 경고 1건

---

## 코드 구조 분석

### 📊 코드 메트릭스

| 항목 | 값 | 비고 |
|------|-----|------|
| 총 라인 수 | 589줄 | |
| 클래스 수 | 1개 | SystemMonitor |
| 메서드 수 | 7개 | |
| 외부 라이브러리 | 6개 | psutil, matplotlib, reportlab, pandas, numpy, GPUtil |

### 🏗️ 코드 구조

```
SystemMonitor 클래스
├── 데이터 수집 계층
│   ├─ collect_data()      : 시스템 메트릭 수집
│   ├─ get_cpu_temp()      : CPU 온도 조회
│   └─ get_gpu_info()      : GPU 정보 조회
│
├── 시각화 계층
│   ├─ setup_plot()        : 그래프 설정 [미사용]
│   └─ update_plot()       : 실시간 그래프 업데이트
│
├── 리포트 생성 계층
│   └─ generate_pdf_report() : PDF 리포트 생성
│
└── 실행 계층
    └─ start()             : 모니터링 시작
```

---

## 보안 문제점

### 🔴 심각도 분류

| 심각도 | 개수 | 비율 |
|--------|------|------|
| 🔴 높음 | 3개 | 30% |
| 🟡 중간 | 4개 | 40% |
| 🟢 낮음 | 3개 | 30% |
| **합계** | **10개** | **100%** |

### 심각도별 분포 그래프

```
높음 (HIGH)    ████████████████████████████████ 30%
중간 (MEDIUM)  ████████████████████████████████████████ 40%
낮음 (LOW)     ████████████████████████████████ 30%
```

---

### 🔴 높음 심각도 (즉시 수정 필요)

#### 1. 경로 조작 취약점 (Path Traversal)

**위치**: 272번째 줄
```python
def generate_pdf_report(self, filename='system_monitor_report.pdf'):
    doc = SimpleDocTemplate(filename, pagesize=A4)  # ❌ 검증 없음
```

**위험도**: 🔴 높음 (CVSS 7.5)

**문제점**:
- 파일명 검증이 전혀 없음
- 시스템의 임의 위치에 파일 생성 가능
- 중요 파일 덮어쓰기 가능

**공격 예시**:
```python
monitor.generate_pdf_report('../../../etc/passwd')
monitor.generate_pdf_report('/root/.ssh/authorized_keys')
```

**수정 방안**:
```python
import os
from pathlib import Path

def generate_pdf_report(self, filename='system_monitor_report.pdf'):
    # 1. 파일명만 추출 (경로 제거)
    filename = os.path.basename(filename)

    # 2. 확장자 검증
    if not filename.endswith('.pdf'):
        filename += '.pdf'

    # 3. 안전한 디렉토리로 제한
    safe_dir = Path('./reports')
    safe_dir.mkdir(exist_ok=True)
    safe_path = safe_dir / filename

    doc = SimpleDocTemplate(str(safe_path), pagesize=A4)
```

---

#### 2. 안전하지 않은 임시 파일 처리

**위치**: 468번째 줄
```python
graph_file = 'temp_graphs.png'  # ❌ 하드코딩
plt.savefig(graph_file, dpi=150, bbox_inches='tight')
# ... 사용 ...
os.remove(graph_file)
```

**위험도**: 🔴 높음 (CWE-377)

**문제점**:
- 동시 실행 시 파일 충돌
- 심볼릭 링크 공격 가능
- Race Condition 취약점

**공격 시나리오**:
```bash
# 터미널 1: 공격자가 심볼릭 링크 생성
ln -s /etc/passwd temp_graphs.png

# 터미널 2: 프로그램 실행
python monitor.py  # /etc/passwd가 덮어써질 수 있음
```

**수정 방안**:
```python
import tempfile

# 안전한 임시 파일 생성
with tempfile.NamedTemporaryFile(suffix='.png', delete=False) as tmp:
    graph_file = tmp.name

try:
    plt.savefig(graph_file, dpi=150, bbox_inches='tight')
    # ... 사용 ...
finally:
    os.unlink(graph_file)  # 항상 삭제
```

---

#### 3. 모든 예외를 무시하는 처리 (Bare Exception)

**위치**: 35줄, 108줄, 122줄

**문제점**:
```python
# 35줄
try:
    import GPUtil
    GPU_AVAILABLE = True
except:  # ❌ KeyboardInterrupt도 잡힘
    GPU_AVAILABLE = False

# 108줄
try:
    temps = psutil.sensors_temperatures()
except:  # ❌ 모든 예외 무시
    pass

# 122줄
try:
    gpus = GPUtil.getGPUs()
except:  # ❌ 오류 추적 불가
    pass
```

**위험도**: 🔴 높음 (CWE-396)

**영향**:
- `KeyboardInterrupt` (Ctrl+C)도 무시됨
- `SystemExit`도 무시됨
- 디버깅 불가능
- 보안 감사 로그 없음

**수정 방안**:
```python
# 구체적인 예외만 처리
try:
    import GPUtil
    GPU_AVAILABLE = True
except ImportError:
    print("GPUtil을 찾을 수 없습니다")
    GPU_AVAILABLE = False
except Exception as e:
    print(f"예상치 못한 오류: {e}")
    GPU_AVAILABLE = False

# 온도 센서
try:
    temps = psutil.sensors_temperatures()
except (AttributeError, PermissionError) as e:
    print(f"온도 센서 접근 불가: {e}")
    return None
```

---

### 🟡 중간 심각도

#### 4. 입력값 검증 부재

**위치**: 43번째 줄
```python
def __init__(self, duration=120):
    self.duration = duration  # ❌ 검증 없음
```

**문제점**:
```python
# 음수 값
monitor = SystemMonitor(duration=-1000)  # 무한 루프

# 매우 큰 값
monitor = SystemMonitor(duration=999999)  # 메모리 고갈

# 잘못된 타입
monitor = SystemMonitor(duration="악의적")  # 타입 오류
```

**수정 방안**:
```python
def __init__(self, duration=120):
    if not isinstance(duration, (int, float)):
        raise TypeError("duration은 숫자여야 합니다")

    if duration <= 0:
        raise ValueError("duration은 양수여야 합니다")

    if duration > 86400:  # 24시간
        raise ValueError("duration은 24시간을 초과할 수 없습니다")

    self.duration = float(duration)
```

---

#### 5. 시스템 정보 노출

**위치**: PDF 리포트 전체

**문제점**:
- CPU/메모리 사용 패턴 노출
- 디스크 I/O 속도 → 스토리지 타입 추정 가능
- 네트워크 대역폭 노출
- 하드웨어 정보 노출

**위험성**:
- 시스템 성능 프로파일링 가능
- 공격 타이밍 파악 가능
- 시스템 용량 및 한계 추정

**완화 방안**:
```python
# PDF 암호화
from reportlab.lib import pdfencrypt

def generate_pdf_report(self, filename, password=None):
    encrypt = None
    if password:
        encrypt = pdfencrypt.StandardEncryption(password, canPrint=1)

    doc = SimpleDocTemplate(filename, pagesize=A4, encrypt=encrypt)
```

---

#### 6. 권한 상승 가능성

**위치**: 67-68번째 줄
```python
self.last_net = psutil.net_io_counters()    # 모든 네트워크 인터페이스
self.last_disk = psutil.disk_io_counters()  # 모든 디스크
```

**문제점**:
- 시스템 전체의 네트워크 통계 수집
- 다른 사용자/프로세스의 활동도 모니터링
- 프라이버시 침해 가능

**수정 방안**:
```python
# 현재 프로세스만 모니터링
import os
current_process = psutil.Process(os.getpid())
process_io = current_process.io_counters()

# 또는 사용자 동의 요청
def get_user_consent():
    print("⚠️  이 도구는 시스템 전체 메트릭을 수집합니다.")
    response = input("계속하시겠습니까? (yes/no): ")
    return response.lower() == 'yes'
```

---

#### 7. 로깅 및 감사 추적 부재

**위치**: 전체 코드

**문제점**:
- 보안 이벤트 기록 없음
- 파일 생성/삭제 로그 없음
- 오류 발생 시 추적 불가
- 누가 언제 실행했는지 알 수 없음

**수정 방안**:
```python
import logging
from datetime import datetime

# 로깅 설정
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('monitor.log'),
        logging.StreamHandler()
    ]
)
logger = logging.getLogger(__name__)

class SystemMonitor:
    def __init__(self, duration=120):
        logger.info(f"모니터링 시작: duration={duration}")
        # ...

    def generate_pdf_report(self, filename):
        logger.info(f"PDF 생성 시작: {filename}")
        try:
            # ... PDF 생성 ...
            logger.info(f"PDF 생성 완료: {filename}")
        except Exception as e:
            logger.error(f"PDF 생성 실패: {e}", exc_info=True)
            raise
```

---

### 🟢 낮음 심각도

#### 8. 하드코딩된 파일명

**위치**: 272줄, 468줄
```python
def generate_pdf_report(self, filename='system_monitor_report.pdf'):  # 하드코딩
graph_file = 'temp_graphs.png'  # 하드코딩
```

**개선안**: 설정 파일로 분리

---

#### 9. 매직 넘버 사용

**위치**: 여러 곳
```python
cpu = psutil.cpu_percent(interval=0.1)  # 0.1은 무엇?
disk_read_rate = (disk.read_bytes - self.last_disk.read_bytes) / (1024 * 1024)  # 1024*1024는?
```

**개선안**: 상수로 정의
```python
BYTES_PER_MB = 1024 * 1024
CPU_SAMPLE_INTERVAL = 0.1
```

---

#### 10. 리소스 누수 가능성

**위치**: 375줄
```python
fig_pdf = plt.figure(figsize=(10, 12))
# ... 처리 ...
plt.close(fig_pdf)  # 예외 발생 시 실행 안 됨
```

**개선안**: try-finally 사용
```python
fig_pdf = plt.figure(figsize=(10, 12))
try:
    # ... 처리 ...
finally:
    plt.close(fig_pdf)
```

---

## 코드 품질 문제점

### 📊 품질 메트릭스

| 항목 | 문제 수 | 심각도 |
|------|---------|--------|
| 코드 중복 | 2개 | 🟡 중간 |
| 미사용 코드 | 1개 | 🟢 낮음 |
| 긴 메서드 | 1개 | 🟡 중간 |
| 복잡도 | 3개 | 🟡 중간 |

---

### 1. 중복된 Import 구문

**위치**: 7-8줄, 28-29줄
```python
# 7-8줄
import os
import sys

# ... 중간 생략 ...

# 28-29줄
import os   # ❌ 중복
import sys  # ❌ 중복
```

**영향**: 가독성 저하, 유지보수 어려움

---

### 2. 미사용 메서드 (Dead Code)

**위치**: 71-95줄
```python
def setup_plot(self):
    """Setup the matplotlib figure with subplots"""
    # 전체 메서드가 어디서도 호출되지 않음
```

**문제점**:
- 정의만 있고 사용되지 않음
- 코드 크기 증가
- 혼란 유발

---

### 3. 너무 긴 메서드

**generate_pdf_report() 메서드 분석**:

| 항목 | 값 | 권장 |
|------|-----|------|
| 라인 수 | 259줄 | 50줄 이하 |
| 순환 복잡도 | 15+ | 10 이하 |

**개선안**: 메서드 분리
```python
def generate_pdf_report(self, filename):
    elements = []
    self._add_title(elements)
    self._add_statistics(elements)
    self._add_graphs(elements)
    self._add_data_table(elements)
    self._build_pdf(filename, elements)

def _add_title(self, elements):
    # 제목 추가 로직
    pass

def _add_statistics(self, elements):
    # 통계 테이블 로직
    pass
```

---

### 4. 테스트 코드 부재

현재 프로젝트 구조:
```
solideo_Day2_09_00/
├── monitor.py
├── requirements.txt
├── README.md
└── ❌ tests/  <-- 없음!
```

**권장 구조**:
```
tests/
├── test_monitor.py
├── test_pdf_generation.py
├── test_data_collection.py
└── test_security.py
```

---

## 성능 분석

### 📊 메모리 사용량

**2분 실행 시**:
```
데이터 저장 (11개 리스트 × 109개 항목 × 8바이트) = ~10KB
```

**24시간 실행 시 (추정)**:
```
109 포인트 / 120초 = 0.908 포인트/초
24시간 = 86,400초
86,400 × 0.908 = 78,451 포인트
78,451 × 8바이트 × 11개 = ~6.7MB
```

**문제점**: 메모리 제한 없음, 장기 실행 시 메모리 고갈 가능

**개선안**:
```python
from collections import deque

class SystemMonitor:
    def __init__(self, duration=120, max_points=10000):
        # 링버퍼 사용 (최대 크기 제한)
        self.timestamps = deque(maxlen=max_points)
        self.cpu_percent = deque(maxlen=max_points)
```

---

### ⚡ 성능 병목

| 위치 | 작업 | 소요 시간 |
|------|------|-----------|
| collect_data() | CPU 샘플링 (0.1초 대기) | ~100ms |
| generate_pdf_report() | 그래프 생성 | ~2-3초 |
| generate_pdf_report() | PDF 빌드 | ~1-2초 |
| **합계** | | **~3-5초** |

---

## 종합 평가 및 권장사항

### 📊 종합 점수

```
┌───────────────────────────────────────────────────┐
│              항목별 점수표                         │
├───────────────────────────────────────────────────┤
│ 항목          │ 점수    │ 가중치 │ 가중 점수       │
├───────────────┼─────────┼────────┼────────────────┤
│ 보안          │ 45/100  │ 40%    │ 18.0           │
│ 코드 품질     │ 65/100  │ 30%    │ 19.5           │
│ 기능성        │ 90/100  │ 20%    │ 18.0           │
│ 성능          │ 80/100  │ 10%    │  8.0           │
├───────────────┴─────────┴────────┼────────────────┤
│ **종합 점수**                    │ **63.5/100**   │
│ **등급**                         │ **C (개선필요)**│
└──────────────────────────────────┴────────────────┘
```

### 점수 시각화

```
보안      ████████████████████░░░░░░░░░░░░░░░░░░░░ 45/100
품질      ████████████████████████████░░░░░░░░░░░░ 65/100
기능      ██████████████████████████████████████░░ 90/100
성능      ████████████████████████████████░░░░░░░░ 80/100
─────────────────────────────────────────────────────
종합      ████████████████████████████░░░░░░░░░░░░ 63.5/100
```

---

### 🎯 개선 로드맵

#### ⚡ 1단계: 긴급 보안 수정 (즉시)

```
작업 시간: 4-6시간
우선순위: CRITICAL

✓ Path Traversal 취약점 수정
✓ Bare Exception을 구체적 예외로 변경
✓ 안전한 임시 파일 처리
✓ 입력값 검증 추가
```

#### 📝 2단계: 코드 품질 개선 (1주)

```
작업 시간: 8-12시간
우선순위: HIGH

✓ 중복 import 제거
✓ 미사용 코드 제거
✓ 긴 메서드 분리 (generate_pdf_report)
✓ 로깅 시스템 구현
✓ 매직 넘버를 상수로 변경
```

#### 🚀 3단계: 기능 강화 (2주)

```
작업 시간: 16-20시간
우선순위: MEDIUM

✓ 단위 테스트 작성 (커버리지 80%+)
✓ PDF 암호화 기능
✓ 설정 파일 지원
✓ CLI 개선
✓ 문서화 개선
```

---

### ✅ 체크리스트

#### 보안 체크리스트

- [ ] 모든 파일 경로 검증
- [ ] 모든 사용자 입력 검증
- [ ] 안전한 임시 파일 사용
- [ ] 구체적인 예외 처리
- [ ] 보안 로깅 구현
- [ ] PDF 암호화 옵션
- [ ] 권한 최소화
- [ ] 보안 스캔 실행 (bandit)

#### 코드 품질 체크리스트

- [ ] 중복 코드 제거
- [ ] 미사용 코드 제거
- [ ] 메서드 크기 축소 (50줄 이하)
- [ ] 단위 테스트 작성
- [ ] 린터 통과 (pylint, flake8)
- [ ] 타입 힌트 추가
- [ ] 문서화 완성

---

### 🛠️ 권장 도구

#### 보안 검사
```bash
# 보안 취약점 스캔
pip install bandit
bandit -r monitor.py

# 의존성 보안 검사
pip install safety
safety check
```

#### 코드 품질
```bash
# 정적 분석
pip install pylint
pylint monitor.py

# 코드 포매팅
pip install black
black monitor.py

# 타입 검사
pip install mypy
mypy monitor.py
```

---

## 💡 결론

### ✅ 강점
1. **기능 완성도**: 모든 요구사항 충족
2. **시각화**: 직관적이고 풍부한 그래프
3. **문서화**: 잘 작성된 README
4. **실행 안정성**: 2분간 안정적으로 실행

### ⚠️ 약점
1. **보안**: 10개의 취약점 (3개 높음, 4개 중간)
2. **테스트**: 단위 테스트 전혀 없음
3. **에러 처리**: Bare Exception 남용
4. **코드 구조**: 긴 메서드, 중복 코드

### 🎯 핵심 개선사항 Top 5

| 순위 | 항목 | 우선순위 | 예상 시간 |
|------|------|----------|-----------|
| 1 | Path Traversal 수정 | 🔴 긴급 | 1시간 |
| 2 | Bare Exception 수정 | 🔴 긴급 | 2시간 |
| 3 | 안전한 임시 파일 | 🔴 긴급 | 1시간 |
| 4 | 로깅 시스템 추가 | 🟡 높음 | 3시간 |
| 5 | 단위 테스트 작성 | 🟡 높음 | 8시간 |

### 📈 개선 후 예상 점수

```
현재 점수:   63.5/100 (C등급)
개선 후:     85.0/100 (B+등급)
───────────────────────────────
향상도:     +21.5 points ⬆️
```

---

**리뷰 완료일**: 2025-11-06
**다음 리뷰**: 개선 완료 후
